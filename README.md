# luduvodb-project/frontend

The frontend for the LuduvoDB project.

## Developing

Once you've lconed the project and installed dependencies with `npm`, `deno`, `bun`, etc, start a development server:

```sh
deno run dev
```
